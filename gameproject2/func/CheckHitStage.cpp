#include "CheckHitStage.h"

bool CheckHitStage::operator()(const CHECK_DIR & dir, const Vector2 & pos, const std::array<int, 4>& offset)
{

	switch (dir)
	{
	case CHECK_DIR::LEFT:

	}

	return false;
}
